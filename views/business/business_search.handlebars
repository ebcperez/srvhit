<form method="post" action="/search/business/filter">
    <div class="form-group">
        <label class="checkbox-inline"><input type="checkbox" value="student" name="filter">Student</label>
        <input type="text" class="form-control" placeholder="zipcode" name="zipcode">
        <button type="submit" class="btn btn-default">Submit</button>
    </div>
</form>
{{#each users}}
    <li class="list-group-item">
        {{#if username}}
        Username: {{username}}<br>
        {{/if}}
        {{#if company_name}}
        Company: {{company_name}}<br>
        {{/if}}
        Email: {{contact_info.email}}<br>
        Account Type: {{account_type}}
    </li>
{{/each}}